# Prompt para Claude en Antigravity Copia y pega esto tal cual en el chat de Antigravity: --- Estoy haciendo una actividad de la materia de Arquitectura de Software sobre Arquitecturas Orientadas por Eventos con Redis Streams. Ya existe un stream `banco.transferencias` con dos consumer groups: `fraude-group` y `notif-group`, que reciben eventos `TransferenciaCreada` publicados con `XADD`. Necesito que me ayudes a completar esto en **tres partes separadas y claramente delimitadas**, en este orden: ## Parte 1 — Infraestructura (Redis CLI / Docker) Dame los comandos para: 1. Verificar/levantar el contenedor Redis (`redis:7`) si no está corriendo. 2. Crear un **tercer consumer group** llamado `auditoria-group` sobre el stream `banco.transferencias`, empezando a leer desde `$` (solo eventos nuevos) o desde `0` si quiero histórico — pregúntame cuál prefiero. 3. Confirmar con `XINFO GROUPS banco.transferencias` que los tres grupos existen. ## Parte 2 — Consumidor de auditoría (Java) Escríbeme una aplicación Java (usando el cliente **Jedis** o **Lettuce**, el que consideres más simple para este caso) que implemente un **consumidor de auditoría** independiente, con esta lógica: - Se conecta a Redis y se registra como consumidor dentro del grupo `auditoria-group`. - Usa `XREADGROUP` en un loop (bloqueante, con `BLOCK`) para leer eventos nuevos del stream `banco.transferencias`. - Por cada evento `TransferenciaCreada` recibido:   - Lo parsea (eventId, transferId, from, to, amount, currency, createdAt).   - Lo **persiste** (puede ser en un archivo de log/JSON local, o en una tabla simple en memoria/H2 — explícame brevemente la opción que elijas y por qué).   - Hace `XACK` solo después de confirmar que se guardó correctamente. - Maneja reconexión básica y logging de errores. - Inclúyeme instrucciones para compilar y ejecutar (Maven/Gradle, según prefieras), y el `pom.xml` o `build.gradle` necesario. Sepárame este código en su propio bloque, claramente etiquetado como "PARTE 2 — Consumidor de auditoría", para no mezclarlo con los comandos de la Parte 1. ## Parte 3 — Simulación de caída antes del XACK Explícame y dame los pasos exactos (comandos `redis-cli`) para simular el escenario que pide la actividad: 1. Publicar un evento `TransferenciaCreada` de prueba con `XADD`. 2. Hacer que un consumidor del `auditoria-group` lo lea con `XREADGROUP` (simulando que "se cae" y **nunca llama a `XACK`**). 3. Mostrar con `XPENDING banco.transferencias auditoria-group` que el evento quedó pendiente, indicando cuánto tiempo lleva sin confirmarse. 4. Usar `XCLAIM` o `XAUTOCLAIM` para que otro consumidor (o el mismo, "reiniciado") reclame ese mensaje pendiente y lo reprocese. 5. Finalmente hacer el `XACK` correcto y verificar con `XPENDING` que ya no queda nada pendiente. Para cada paso dame también una explicación corta de qué está pasando internamente en Redis (en términos del PEL — Pending Entries List), para que pueda explicarlo en la entrega. ## Formato de respuesta esperado - Usa encabezados claros (Parte 1, Parte 2, Parte 3) tal como te los planteé. - El código Java debe ir en bloques de código separados por archivo (ej: `pom.xml`, `AuditoriaConsumer.java`). - Al final, dame un resumen tipo checklist de qué archivos/comandos generaste y en qué orden ejecutarlos para correr toda la demo de punta a punta. ---

me ayudas a hacer esto? necesito igual que trabajes en carpetas oranizado
 